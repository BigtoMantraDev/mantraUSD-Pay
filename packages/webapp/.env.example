# This value will be overwritten on build time (commit hash for preview, semver for prod)
VITE_FRONTEND_VERSION=v1.0.0

# Optional: WalletConnect Project ID (falls back to default if not provided)
# Get your own from https://cloud.reown.com/
# VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Counter Contract Address (EVM)
# Deploy the Counter contract and add its address here
# For local: Deploy using `yarn contracts:deploy:local` from root
# For testnet: Deploy using `yarn contracts:deploy:dukong` from root
# For mainnet: Deploy using `yarn contracts:deploy:mainnet` from root
VITE_COUNTER_CONTRACT_ADDRESS=0x1D189ab375FA6FBF5436DD09993da044c0504979

# Local Development Chain
# When running in development mode, a local chain option will be available
# Make sure your local MANTRA chain is running on http://localhost:8545
# Chain ID: 1337 (EVM) / mantra-local-1 (Cosmos)